namespace roq.fbs;

table CancelOrder {
  account:string;
  zzz_order_id:uint32 (deprecated);
  routing_id:string;
  version:uint32;
  conditional_on_version:uint32;
  request_template:string;
  order_id:uint64;
}
