include "MBOUpdate.fbs";
include "Origin.fbs";
include "TimeSeriesType.fbs";

namespace roq.fbs;

table TimeSeriesUpdate {
  stream_id:uint16;
  exchange:string;
  symbol:string;
  origin:Origin;
  type:TimeSeriesType;
  frequency:uint8;
  bars:[Bar];
  update_type:UpdateType;
}
