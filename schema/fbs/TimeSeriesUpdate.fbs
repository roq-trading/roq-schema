include "Bar.fbs";
include "Origin.fbs";
include "TimeSeriesSource.fbs";
include "UpdateType.fbs";

namespace roq.fbs;

table TimeSeriesUpdate {
  stream_id:uint16;
  exchange:string;
  symbol:string;
  source:TimeSeriesSource;
  frequency:uint8;
  origin:Origin;
  bars:[Bar];
  update_type:UpdateType;
  exchange_time_utc:int64;
}
