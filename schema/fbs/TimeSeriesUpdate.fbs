include "Bar.fbs";
include "DataSource.fbs";
include "Interval.fbs";
include "Origin.fbs";
include "UpdateType.fbs";

namespace roq.fbs;

table TimeSeriesUpdate {
  stream_id:uint16;
  exchange:string;
  symbol:string;
  data_source:DataSource;
  interval:Interval;
  origin:Origin;
  bars:[Bar];
  update_type:UpdateType;
  exchange_time_utc:int64;
}
