syntax = "proto3";

package roq.proto;

enum BufferCapacity {
  BUFFER_CAPACITY_UNSPECIFIED = 0;
  BUFFER_CAPACITY_EMPTY = 1;
  BUFFER_CAPACITY_LOW_WATER_MARK = 2;
  BUFFER_CAPACITY_HIGH_WATER_MARK = 3;
  BUFFER_CAPACITY_FULL = 4;
}
